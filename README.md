# storefront

a simple storefront and configuration/customization web app created to explore
WebAssembly with Go.

## building and running

1. run `go generate ./...`
2. change into /server with `cd server`
3. run `go run .`

then navigate to localhost:8080.
